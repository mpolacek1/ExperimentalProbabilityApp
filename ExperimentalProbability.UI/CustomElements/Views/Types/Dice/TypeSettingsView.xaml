<UserControl x:Class="ExperimentalProbability.UI.CustomElements.Views.Types.Dice.TypeSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExperimentalProbability.UI.CustomElements.Views.Types.Dice"
             mc:Ignorable="d"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Visibility="Collapsed">

    <WrapPanel Margin="0,0,0,5">
        <Label Target="{Binding DiceSelection}" Style="{StaticResource Style_Label_DiceSelection}"/>
        <ComboBox x:Name="DiceSelection" Style="{StaticResource Style_ComboBox_DiceSelection}" ItemsSource="{Binding DiceTypes.Keys}" SelectionChanged="DiceSelection_SelectionChanged"/>
    </WrapPanel>

</UserControl>
